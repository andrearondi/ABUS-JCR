"""Scoring oracle: byte-identical vendored ``det_score.py`` + a thin wrapper.

See :mod:`abus_jcr.eval.froc` for the public API. The vendored copy
``_official_det_score.py`` is never edited; a drift test asserts it stays
byte-identical to ``Final_Evaluation/det_score.py``.
"""
